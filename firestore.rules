rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ DEVELOPMENT RULES - NOT FOR PRODUCTION ⚠️
    // These rules allow unrestricted access for testing
    // Deploy firestore.rules.production for production use
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
