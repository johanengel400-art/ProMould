rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for now (development mode)
    // TODO: Implement proper authentication and restrict access
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
