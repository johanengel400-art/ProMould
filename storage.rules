rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // ⚠️ DEVELOPMENT RULES - NOT FOR PRODUCTION ⚠️
    // These rules allow unrestricted access for testing
    // Deploy storage.rules.production for production use
    
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
